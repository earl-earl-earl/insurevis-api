pyyaml==5.1
opencv-python
# Use pre-built wheel from Facebook's index
detectron2 # <--- Just the package name now
torch==1.10.0+cpu # <--- Add/ensure correct PyTorch CPU version
torchvision==0.11.0+cpu # <--- Add/ensure compatible torchvision CPU version
onnxruntime
shapely
matplotlib # Consider if truly needed for the deployed API, often used for local plotting
pillow
wheel # <--- Keep wheel, good practice

# --- Dependencies for Flask API and Render Deployment ---
Flask
gunicorn
